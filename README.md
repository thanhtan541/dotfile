# dev-config

## Setup

```
# Init submodule 
$ git submodule update --init
# Enable submodule diff log
$ git config --global diff.submodule log
# Update your submodules
$ git submodule update --remote your_submodule(s)
# setup everything
$ sh setup.sh
```
